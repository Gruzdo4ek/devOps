version: '3.8'

services:
  backend:
    image: localhost:5001/calculator-backend:latest  # ← было 5000
    ports:
      - "5000:5000"  # хост:контейнер (Flask внутри контейнера слушает 5000)

  frontend:
    image: localhost:5001/calculator-frontend:latest  # ← было 5000
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - app-network

networks:
  app-network:
    driver: bridge